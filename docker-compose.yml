services:
  rk9:
    build:
      context: .

    container_name: rk9

    environment:
      # Your Discord bot's token
      RK9_DISCORD_TOKEN: ""
      # How often, in minutes, e621 is queried for new posts. The default is 15.
      #RK9_CHECK_INTERVAL: 15

    volumes:
      # Where /rk9/ should store data.
      - "./data:/data"